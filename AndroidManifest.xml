<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tricorderdemo"
    android:versionCode="1"
    android:versionName="1.0" >
<!-- 
README
TricorderDemo 
Date: 09/18/14

Known Bugs:
	-Tricorder crashes after attempting to resume, after app has been minimized and reopened.
	-Tricorder continues to run when minimized, sapping power and overall hardware performance.

Extensions:
	-Some kind of close app button.
	-New and Improved GUI.
	-Some actual documentation.

Signed-off-by: dnmorris7 <dnmorris7@gmail.com>-->

	<!-- Random Comments -->
    <uses-sdk
        android:minSdkVersion="1"
        android:targetSdkVersion="18" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.tricorderdemo.Tricorder"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
